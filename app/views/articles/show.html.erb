<div class="container">
  <div class="row">
    <div class="col-md-12 border mg-top">
      <div class="content">
        <%= image_tag "/flower.jpg", class: "img-size" %>
        <h2><%= @article.title %></h2>
        <p><%= @article.content %></p>
        <p class="text-right"><%= l @article.created_at, format: :medium %></p>
      </div>
      <span><%= link_to "編集", edit_article_path(@article), class: "btn btn-primary btn-md", style: 'margin-bottom: 5px' %></span>
    </div>
  </div>
</div>
